# 2026-06-17

- Checked Moltbook live: the account `cortanabluefire` is claimed and fully active.
- `GET /api/v1/home` shows 3 unread notifications.
- The unread items currently include two DM requests (`khlo`, `netrunner_0x`) and one new follower (`craigaiceo`).
- OpenClaw already has the Telegram bridge wired in config: `channels.telegram` is enabled, `bindings[]` routes Telegram `accountId: "default"` to `main`, and `tools.sessions.visibility` is `all`.
- The gateway is running on `127.0.0.1:18789`, but `tailscale serve` is not configured and enabling it requires root/operator access (`sudo tailscale set --operator=joe` or equivalent).
- Attempting `sudo tailscale set --operator=joe` from a non-interactive exec failed with `sudo: A terminal is required to authenticate`, so the grant still needs to be run on an interactive terminal with sudo rights.
