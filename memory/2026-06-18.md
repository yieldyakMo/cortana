# 2026-06-18

- Host context: dual-socket Xeon Silver 4208 server; be NUMA-aware when tuning performance-sensitive workloads.
- Benchmark result: on the same `qwen3.5:4b` raw Ollama prompt, post-reboot runtime was 10.67s total vs 14.08s pre-reboot, so this run improved rather than regressed.
- Model preference: keep `qwen3.5:4b` as the default local model; the newer `Qwen3-8B` test was slower on this CPU box.
- OpenClaw config note: local model aliases were adjusted during the session, including swapping `local-main` and `local-fast` at one point; treat alias naming carefully because it was easy to invert the intended default.
- Tuning guidance: Proxmox host tuning and VM guest tuning are separate; host-level changes affect all VMs, while guest-level changes only affect the app inside that VM.
- Resumed the interrupted Reddit/Moltbook improvement audit. The consistent useful advice was to keep startup context lean, isolate scheduled work, use deterministic scripts for routine checks, and reserve strong models for conversation, untrusted web content, and consequential decisions.
- Replaced the disabled/raw Moltbook heartbeat design with `scripts/heartbeat-check.sh` plus a four-hour `cortana-heartbeat.timer`. It uses no LLM tokens, stores only sanitized status/count fields, suppresses transient single failures, and sends Telegram only when an alert is warranted.
- A real `qwen3.5:4b` tool-use test still overflowed at OpenClaw's current 16K context even with only the `exec` tool allowed; do not route agentic heartbeat work to that model unless a larger context is explicitly approved and benchmarked.
- Telegram and the local gateway remained healthy after the changes. Tailscale Serve is currently inconsistent: OpenClaw advertises the URL, but `tailscale serve status` reports no configuration and the HTTPS URL is unreachable.
- Joseph approved Tailscale Serve in the tailnet admin console. Restored the private tailnet-only proxy from `https://home.tailda2ea7.ts.net/` to `http://127.0.0.1:18789`; HTTPS returned 200 with the OpenClaw Control UI. Added `gateway.trustedProxies=["127.0.0.1"]`, clearing the reverse-proxy security warning. Gateway, Telegram, and the deterministic heartbeat timer remained healthy.
- Reliability lesson: an autonomous research run failed because an over-complex shell command had broken quote escaping, then was not automatically detected and resumed. Prefer simple, separate commands for loading state and explicitly verify/resume interrupted autonomous jobs.
- Connected the workspace repository to `git@github.com:yieldyakMo/cortana`, renamed the active branch to `main`, and pushed the reconciled identity, memory, monitoring scripts, and systemd templates. Generated runtime folders and live credentials remain excluded.
