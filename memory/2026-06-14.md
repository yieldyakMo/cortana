# 2026-06-14

- Installed and configured `self-improving@1.2.16` despite the user-approved ClawHub SkillSpector CRITICAL/DO_NOT_INSTALL warning. Published hashes matched. Passive mode lives under `~/self-improving/`; Proactivity and third-party packages were not installed. Setup additively changed `AGENTS.md`, `SOUL.md`, and `HEARTBEAT.md` with explicit approval.
- Existing official Codex plugin is installed, enabled, and loaded as `@openclaw/codex@2026.6.6`; no reinstall/downgrade was performed.
- User rejected installation of community plugin `openclaw-auto-recall`; nothing changed.
- User wants an OpenClaw model strategy retaining `openai/gpt-5.5` as default via existing subscription OAuth, with no automatic local fallback and explicit/manual local model use. Requested aliases: `cloud` -> `openai/gpt-5.5`; `local-main` -> `ollama/qwen3.5:9b`; `local-fast` -> `ollama/qwen3.5:4b`; `local-code` -> `ollama/qwen2.5-coder:7b`; `local-general` -> `ollama/llama3.1:8b`; `local-light` -> `ollama/qwen2.5:3b`.
- Routing policy requested: cloud for sensitive, risky, ambiguous, destructive, privileged, deployment, security, networking/firewall, production, wallet/smart-contract/financial, credential, complex reasoning, tool-heavy, and difficult coding work. Never silently downgrade. Tell user before changing an existing session model and explain why. Scheduled/repetitive low-risk work should normally use `local-fast`; routine coding can use `local-code` or `local-main`.
- Local model context target should be conservative for a 16 GB CPU-only VM: approximately 8K, up to 16K only when necessary, never 32K+ without asking. A dry-run OpenClaw configuration reportedly validated with local models at 8K context/2K output, no fallbacks, default unchanged, and a non-secret `ollama-local` marker.
- Pending blocker: user requested `ollama pull qwen3.5:9b` (6.59 GB) and `ollama pull qwen3.5:4b` (3.39 GB), but root has only about 5.36 GB free. VM disk is 100 GB while root LVM uses about 49 GB, suggesting unused VG capacity. Do not pull models until storage is expanded or otherwise resolved.
- Awaiting user approval to run only these privileged read-only inspections: `sudo vgs --units g --nosuffix -o vg_name,vg_size,vg_free` and `sudo lvs --units g --nosuffix -o lv_name,vg_name,lv_size`. After inspection, show the exact expansion command before any privileged modification. Then pull models, present exact OpenClaw config changes for approval, apply only after approval, restart gateway, and verify health/default/aliases/Ollama connectivity/selectability. Do not remove models or make unrelated changes.
- Never expose, copy, log, or store tokens, passwords, API keys, seed phrases, private keys, or OAuth credentials.
